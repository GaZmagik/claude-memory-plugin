# Testing Rules

## Test Coverage
- All functions must have corresponding tests
- Tests must be in parallel test files
- Use Bun test framework

## TDD Workflow
1. Write failing test (Red)
2. Write minimal code to pass (Green)
3. Refactor (Refactor)
